# alvr_gui

Crate for GUI-related code. It needs a backend to display the UI.

## Languages

Dashboard translations are stored in `resources/languages`. The dashboard uses
the system locale automatically and supports Simplified Chinese (`zh-CN`). Set
`ALVR_LANGUAGE=zh-CN` to force Chinese, or add entries to the language files
when translating additional dashboard text.
